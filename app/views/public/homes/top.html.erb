  <div class='container'>
    <div class='row'>
      <div class='col-12'>
        <h4>
          ようこそ、ながのCAKEへ！<br>
          このサイトは、ケーキ販売のECサイトになります。<br>
          会員でない方も商品の閲覧はできますが、<br>
          購入には会員登録が必要になります。
        </h4>
      </div>
    </div>
      <div class='row'>
        <div class='col-12'>
          <h4>新着商品</h4>
        </div>
        <div class='col-4 d-flex'>
            <% @items.each do |item| %>
              <div>
                <% if item.image.attached? %>
                  <%= link_to image_tag(item.image, size: "200x150"), item_path(item.id) %>
                <% else %>
                  <%= link_to image_tag('no_image', size: "100x100"), item_path(item.id) %>
                <% end %><br>
                <%= item.name %><br>
                ¥<%= item.price.to_s(:delimited) %>
              </div>
            <% end %>
        </div>
      </div>
      <div class="text-right">
        <%= link_to '全ての商品を見る＞', items_path %>
      </div>
  </div>